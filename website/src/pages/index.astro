---
import BaseLayout from "../layouts/BaseLayout.astro";
import CopyButton from "../components/CopyButton.astro";
import ChevronDownIcon from "lucide-astro/ChevronDown";
import ExternalLinkIcon from "lucide-astro/ExternalLink";

const homeStructuredData = {
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  name: "Tako",
  applicationCategory: "DeveloperApplication",
  operatingSystem: "macOS, Linux",
  url: "https://tako.sh/",
  image: "https://tako.sh/assets/og.png",
  description: "Run apps on your own servers with local development, production deploys, routing, TLS, logs, secrets, and more.",
  softwareHelp: "https://tako.sh/docs/",
  downloadUrl: "https://tako.sh/install.sh",
  codeRepository: "https://github.com/tako-sh/tako",
  featureList: [
    "Local development with HTTPS and custom domains",
    "Production deploys with rolling updates",
    "Automatic TLS and request routing",
    "Logs, secrets, and server management",
    "Public image optimization",
  ],
};
---
<BaseLayout
  title="Run apps on your own servers - Tako"
  description="Deploy apps to your own servers with rolling deploys, TLS, health checks, logs, and secrets for Node.js, Bun, Go, and more."
  structuredData={homeStructuredData}
>
  <section class="hero">
    <div
      class="panel-hanger"
      id="mit-hanger"
      aria-label="License: MIT (includes Apache-2.0 dependencies)"
      title="Project code is MIT; some dependencies (for example Pingora) are Apache-2.0. See THIRD_PARTY_NOTICES.md."
    >
      <span class="panel-pin" id="mit-pin" aria-hidden="true"></span>
      <div class="panel-arm">
        <span class="panel-thread"></span>
        <div class="panel-tag" id="mit-tag">
          <span class="panel-tag-label">license</span>
          <span class="panel-tag-value">MIT</span>
        </div>
      </div>
    </div>
    <h1 class="title">Your app <span class="title-mid">belongs<br /> on</span> <span class="accent">your servers</span></h1>
    <p class="subtitle">
      Everything you need to build, deploy, and run your app.
    </p>

    <div class="command-card">
      <div class="command-head">
        <span class="command-caption">install with one command</span>
      </div>
      <div class="command-body">
        <span class="prompt">$</span>
        <span class="cmd" id="typed-cmd"></span>
        <span class="cursor" aria-hidden="true"></span>
        <CopyButton class="copy-inline" id="copy" ariaLabel="copy install command" title="copy install command" label="copy" />
      </div>
    </div>
    <div class="hero-actions">
      <a class="btn btn-primary" href="/docs/quickstart/">get started</a>
      <a class="cta-link hero-actions-link hero-demo-link" href="https://demo.tako.sh" target="_blank" rel="noopener noreferrer">
        <span>live demo</span>
        <ExternalLinkIcon class="hero-demo-icon" aria-hidden="true" focusable="false" />
      </a>
      <a class="cta-link hero-actions-link" href="/docs/">or read the docs &rarr;</a>
    </div>
  </section>

  <section class="philosophy-section">
    <h2 class="philosophy-label">Takosophy</h2>
    <p class="philosophy-text">
      Full <strong class="accent-coral">control.</strong> A real server, a real runtime &mdash; what runs locally runs in production.
    </p>
    <p class="philosophy-text">
      Unapologetically <strong class="accent-mint">opinionated.</strong> No config rabbit holes &mdash; Tako picks the defaults so you can ship.
    </p>
    <p class="philosophy-text">
      Built for the <strong class="accent-gold">impatient.</strong> Every step is optimized to get your code live fast.
    </p>
  </section>

  <section class="workflow-section">
    <div class="hero-block-head">
      <h2 class="hero-block-label">Three commands and you're live</h2>
      <span class="hero-block-rule" aria-hidden="true"></span>
    </div>
    <ol class="workflow-steps">
      <li class="workflow-step">
        <a href="/docs/quickstart/" class="workflow-link">
          <span class="workflow-orb workflow-orb-1" aria-hidden="true">1</span>
          <code class="workflow-cmd">tako init</code>
          <p class="workflow-desc">Detect runtime, create config</p>
        </a>
      </li>
      <li class="workflow-step">
        <a href="/docs/development/" class="workflow-link">
          <span class="workflow-orb workflow-orb-2" aria-hidden="true">2</span>
          <code class="workflow-cmd">tako dev</code>
          <p class="workflow-desc">Develop locally with HTTPS</p>
        </a>
      </li>
      <li class="workflow-step">
        <a href="/docs/deployment/" class="workflow-link">
          <span class="workflow-orb workflow-orb-3" aria-hidden="true">3</span>
          <code class="workflow-cmd">tako deploy</code>
          <p class="workflow-desc">Build, upload, rolling update</p>
        </a>
      </li>
    </ol>
  </section>

  <section class="features-section">
    <div class="hero-block-head">
      <h2 class="hero-block-label">What's inside</h2>
      <span class="hero-block-rule" aria-hidden="true"></span>
    </div>
    <p class="features-subcopy">Each usually needs its own tool. Tako keeps them in one deploy flow.</p>
    <div class="included-tags" role="group" aria-label="Filter features by category">
      <button class="included-tag" data-category="server" type="button">server</button>
      <button class="included-tag" data-category="process" type="button">process management</button>
      <button class="included-tag" data-category="deploy" type="button">build &amp; deploy</button>
      <button class="included-tag" data-category="development" type="button">development</button>
      <button class="included-tag" data-category="media" type="button">media</button>
    </div>
    <div class="feature-list">
      <!-- server -->
      <a href="/docs/how-tako-works/" class="feature-link" data-categories="server">
        <span class="feature-link-title">Reverse proxy</span>
        <span class="feature-link-desc">Pingora-based with sensible defaults, zero config needed</span>
      </a>
      <a href="/docs/how-tako-works/#tls" class="feature-link" data-categories="server">
        <span class="feature-link-title">Automatic TLS</span>
        <span class="feature-link-desc">Let's Encrypt with wildcard support, auto-renewed</span>
      </a>
      <a href="/docs/how-tako-works/#app-processes" class="feature-link" data-categories="server">
        <span class="feature-link-title">Health-based routing</span>
        <span class="feature-link-desc">Unhealthy instances get pulled from rotation automatically</span>
      </a>
      <a href="/docs/how-tako-works/#scale-to-zero" class="feature-link" data-categories="server,process">
        <span class="feature-link-title">Scale to zero and back</span>
        <span class="feature-link-desc">Idle apps sleep, the next request wakes them</span>
      </a>
      <a href="/docs/deployment/#images" class="feature-link" data-categories="server,media">
        <span class="feature-link-title">Image optimization <span class="new-badge">new</span></span>
        <span class="feature-link-desc">Resize and serve WebP, with AVIF available when enabled</span>
      </a>
      <span class="feature-link feature-link-soon" data-categories="server,deploy,media">
        <span class="feature-link-title">Uploads and file storage <span class="soon-badge">soon</span></span>
        <span class="feature-link-desc">Signed browser uploads, private downloads, and public URLs backed by S3/R2</span>
      </span>
      <a href="/docs/deployment/" class="feature-link" data-categories="server,deploy">
        <span class="feature-link-title">Static files</span>
        <span class="feature-link-desc">Drop files in public/ &mdash; Tako serves them directly</span>
      </a>
      <!-- build & deploy -->
      <a href="/docs/deployment/" class="feature-link" data-categories="deploy">
        <span class="feature-link-title">Build locally, deploy anywhere</span>
        <span class="feature-link-desc">No CI required &mdash; build and deploy with a single command</span>
      </a>
      <a href="/docs/deployment/" class="feature-link" data-categories="server,deploy,process">
        <span class="feature-link-title">Rolling deploys</span>
        <span class="feature-link-desc">New instances first, traffic shifts after health checks</span>
      </a>
      <a href="/docs/deployment/#rolling-updates" class="feature-link" data-categories="deploy,process">
        <span class="feature-link-title">Instant rollback</span>
        <span class="feature-link-desc">Previous version stays ready, one command to revert</span>
      </a>
      <a href="/docs/deployment/" class="feature-link" data-categories="server,deploy">
        <span class="feature-link-title">Environments &amp; servers</span>
        <span class="feature-link-desc">Group servers by environment, deploy to the right one</span>
      </a>
      <a href="/docs/how-tako-works/" class="feature-link" data-categories="server">
        <span class="feature-link-title">Bun, Node, Go &mdash; you name it</span>
        <span class="feature-link-desc">Runtimes detected and managed automatically</span>
      </a>
      <a href="/docs/how-tako-works/" class="feature-link" data-categories="server,deploy">
        <span class="feature-link-title">Runtime on autopilot</span>
        <span class="feature-link-desc">Runtimes and package managers pinned and managed for you</span>
      </a>
      <a href="/docs/presets/" class="feature-link" data-categories="deploy,development">
        <span class="feature-link-title">Presets</span>
        <span class="feature-link-desc">Framework-aware defaults for Next.js, TanStack Start, and more</span>
      </a>
      <!-- development -->
      <a href="/docs/development/" class="feature-link" data-categories="development">
        <span class="feature-link-title">Local DNS</span>
        <span class="feature-link-desc">Each app gets a real domain automatically &mdash; no ports to remember</span>
      </a>
      <a href="/docs/development/" class="feature-link" data-categories="development">
        <span class="feature-link-title">.local domains</span>
        <span class="feature-link-desc">Access any app from your phone, TV, or any device on your network</span>
      </a>
      <a href="/docs/development/" class="feature-link" data-categories="development">
        <span class="feature-link-title">Wildcard routing</span>
        <span class="feature-link-desc">Subdomains for every app and branch, resolved automatically</span>
      </a>
      <a href="/docs/development/" class="feature-link" data-categories="development">
        <span class="feature-link-title">Always-on dev</span>
        <span class="feature-link-desc">Never stop a server &mdash; idle apps cost nothing, wake when you need them</span>
      </a>
      <a href="/docs/development/" class="feature-link" data-categories="development">
        <span class="feature-link-title">Agent-friendly</span>
        <span class="feature-link-desc">Each worktree gets its own URL</span>
      </a>
      <!-- process management -->
      <a href="/docs/cli/" class="feature-link" data-categories="process">
        <span class="feature-link-title">Logs</span>
        <span class="feature-link-desc">Stream live or pull recent history across all servers</span>
      </a>
      <a href="/docs/cli/" class="feature-link" data-categories="process,deploy">
        <span class="feature-link-title">Secrets</span>
        <span class="feature-link-desc">Encrypted locally and on servers; keys can sync via iCloud Keychain</span>
      </a>
      <a href="/docs/cli/" class="feature-link" data-categories="process,server">
        <span class="feature-link-title">Inspect &amp; monitor</span>
        <span class="feature-link-desc">Running instances, deploy history, all at a glance</span>
      </a>
      <a href="/docs/how-tako-works/#workflows-and-channels" class="feature-link" data-categories="server">
        <span class="feature-link-title">Durable channels <span class="new-badge">new</span></span>
        <span class="feature-link-desc">WebSocket and SSE routes with bounded replay</span>
      </a>
      <a href="/docs/how-tako-works/#workflows-and-channels" class="feature-link" data-categories="server">
        <span class="feature-link-title">Durable workflows <span class="new-badge">new</span></span>
        <span class="feature-link-desc">Long-running tasks that survive restarts</span>
      </a>
    </div>
    <button type="button" class="feature-show-all" data-feature-show-all>
      <span class="feature-show-all-label">show more</span>
      <ChevronDownIcon class="feature-show-all-icon" aria-hidden="true" focusable="false" />
    </button>
  </section>

  <section class="performance-section">
    <div class="hero-block-head">
      <h2 class="hero-block-label">Ready for serious load</h2>
      <span class="hero-block-rule" aria-hidden="true"></span>
    </div>
    <div class="performance-copy">
      <p>
        Tako can handle <span class="performance-mark performance-traffic">heavy traffic</span> even on modest hardware: one small VM served 12.5k HTTPS 200 RPS at 5k concurrency, with <span class="performance-mark performance-clean">zero client errors</span> up to 20k concurrency.
        In the same public run, it outpaced <a href="https://www.envoyproxy.io/" class="performance-reference-link" target="_blank" rel="noopener noreferrer nofollow">Envoy</a> and <a href="https://caddyserver.com/" class="performance-reference-link" target="_blank" rel="noopener noreferrer nofollow">Caddy</a> under heavy HTTPS load.
        <a href="/performance/" class="performance-link">Read report &rarr;</a>
      </p>
    </div>
  </section>

  <section class="cta-section">
    <p class="cta-text">Stop configuring.<br class="mobile-break"> Start shipping.</p>
    <div class="cta-actions">
      <a href="/docs/quickstart/" class="btn cta-btn">get started</a>
      <a href="/docs/how-tako-works/" class="cta-link">or read how Tako works &rarr;</a>
    </div>
  </section>
</BaseLayout>

<style is:global>
  .hero {
    border-radius: var(--panel-radius);
    border: 2px solid var(--line);
    background: var(--panel);
    box-shadow: 0 18px 45px rgba(47, 42, 68, 0.1);
    padding: clamp(24px, 5vw, 54px);
    overflow: visible;
    position: relative;
    isolation: isolate;
  }

  .hero::before {
    content: "";
    position: absolute;
    inset: 0;
    border-radius: inherit;
    background:
      radial-gradient(ellipse at 85% 25%, rgba(155, 196, 182, 0.16) 0%, transparent 50%),
      radial-gradient(ellipse at 70% 75%, rgba(232, 135, 131, 0.14) 0%, transparent 45%),
      radial-gradient(ellipse at 95% 60%, rgba(214, 166, 62, 0.10) 0%, transparent 40%),
      radial-gradient(ellipse at 30% 45%, rgba(232, 135, 131, 0.08) 0%, transparent 50%),
      radial-gradient(ellipse at 55% 80%, rgba(155, 196, 182, 0.10) 0%, transparent 50%),
      radial-gradient(ellipse at 20% 65%, rgba(214, 166, 62, 0.055) 0%, transparent 45%);
    z-index: 0;
    pointer-events: none;
  }

  .hero::after {
    content: "";
    position: absolute;
    inset: 0;
    border-radius: inherit;
    background: url("data:image/svg+xml,%3Csvg viewBox='0 0 1024 1024' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
    opacity: 0.09;
    mix-blend-mode: overlay;
    pointer-events: none;
    z-index: 1;
  }

  .hero > *:not(.panel-hanger) {
    position: relative;
    z-index: 2;
  }

  .panel-hanger {
    position: absolute;
    top: 26px;
    right: 10px;
    width: 164px;
    height: 152px;
    z-index: 12;
    --swing-angle: 0.17rad;
    --thread-length: 48px;
    --card-tilt: -0.06rad;
    --hanger-axis: 112px;
  }

  .panel-pin {
    position: absolute;
    top: 10px;
    left: calc(var(--hanger-axis) - 5px);
    width: 10px;
    height: 10px;
    border: 2px solid rgba(47, 42, 68, 0.28);
    border-radius: 50%;
    background: var(--secondary);
    box-shadow: 0 2px 0 rgba(47, 42, 68, 0.15);
  }

  .panel-arm {
    position: absolute;
    top: 16px;
    left: var(--hanger-axis);
    width: 0;
    transform-origin: top center;
    transform: rotate(var(--swing-angle));
    pointer-events: none;
  }

  .panel-thread {
    display: block;
    width: 2px;
    margin-left: -1px;
    height: var(--thread-length);
    background: rgba(47, 42, 68, 0.45);
  }

  .panel-tag {
    position: absolute;
    left: 0;
    top: var(--thread-length);
    transform: translateX(-50%) rotate(var(--card-tilt));
    transform-origin: top center;
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    width: 86px;
    border: 2px solid rgba(47, 42, 68, 0.26);
    border-radius: 12px 12px 16px 16px;
    background: linear-gradient(160deg, var(--panel-strong), var(--panel));
    box-shadow: 0 8px 14px rgba(47, 42, 68, 0.16);
    padding: 8px 10px 10px;
    font-family: "IBM Plex Mono", ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    font-size: 12px;
    color: rgba(47, 42, 68, 0.86);
    text-align: center;
    z-index: 1;
    pointer-events: auto;
    cursor: grab;
    touch-action: none;
    user-select: none;
  }

  .panel-tag-label {
    display: block;
    font-size: 11px;
    letter-spacing: 0.1em;
    line-height: 1;
    opacity: 0.82;
    text-transform: uppercase;
  }

  .panel-tag-value {
    display: block;
    margin-top: 2px;
    font-size: 18px;
    font-weight: 700;
    letter-spacing: 0.08em;
    line-height: 1;
    color: var(--primary-text);
  }

  .title {
    font-family: var(--font-heading);
    font-size: clamp(38px, 8vw, 72px);
    line-height: 1;
    letter-spacing: 0.01em;
    margin-bottom: 14px;
  }

  .title .accent {
    color: var(--accent);
  }

  .subtitle {
    max-width: 680px;
    font-size: clamp(18px, 2.8vw, 26px);
    line-height: 1.25;
    color: var(--muted);
    margin-bottom: 24px;
  }

  .command-card {
    border: 2px solid rgba(47, 42, 68, 0.2);
    border-radius: 18px;
    background: var(--paper);
    overflow: hidden;
    max-width: 640px;
  }

  .command-head {
    display: flex;
    align-items: center;
    justify-content: flex-start;
    position: relative;
    background: var(--mint-soft);
    border-bottom: 1.5px solid rgba(47, 42, 68, 0.1);
    padding: 7px 14px;
  }

  .command-caption {
    font-family: "IBM Plex Mono", ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.08em;
    color: rgba(47, 42, 68, 0.6);
  }

  .command-body {
    font-family: "IBM Plex Mono", ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    font-size: clamp(14px, 2.1vw, 18px);
    padding: 14px 14px 14px 16px;
    display: flex;
    flex-wrap: nowrap;
    gap: 10px;
    align-items: center;
    justify-content: flex-start;
    overflow-x: auto;
  }

  .prompt {
    color: var(--primary-text);
  }

  .cmd {
    color: var(--ink);
    min-height: 1.2em;
    white-space: nowrap;
    flex: 0 0 auto;
  }

  .cursor {
    width: 8px;
    height: 18px;
    background: var(--ink);
    animation: blink 1s steps(1, end) infinite;
    flex: 0 0 auto;
  }

  .hero-actions {
    display: flex;
    align-items: center;
    gap: 10px 12px;
    flex-wrap: wrap;
    margin-top: 18px;
  }

  .hero-actions-link {
    font-size: 15px;
  }

  .hero-demo-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    min-height: 45px;
    padding: 0 18px;
    border: 1.5px solid rgba(47, 42, 68, 0.18);
    border-radius: 999px;
    background: var(--panel-glass);
    box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--panel-strong) 72%, transparent);
    font-weight: 700;
    opacity: 1;
    transition: background 0.15s ease, border-color 0.15s ease, color 0.15s ease, transform 0.15s ease, box-shadow 0.15s ease;
  }

  .hero-demo-link:hover {
    transform: translateY(-1px);
    border-color: rgba(155, 196, 182, 0.72);
    background: rgba(219, 235, 230, 0.62);
    color: var(--ink);
    box-shadow: 0 5px 14px rgba(80, 113, 102, 0.14);
  }

  .hero-demo-icon {
    width: 0.95em;
    height: 0.95em;
    stroke-width: 2.4;
  }

  .btn {
    border: 1.5px solid rgba(232, 135, 131, 0.4);
    border-radius: 999px;
    background: rgba(232, 135, 131, 0.2);
    color: var(--ink);
    padding: 8px 18px;
    font-family: "Nunito", sans-serif;
    font-size: 16px;
    font-weight: 700;
    line-height: 1;
    text-decoration: none;
    cursor: pointer;
    transition: background 0.15s ease, border-color 0.15s ease, transform 0.15s ease, box-shadow 0.15s ease;
  }

  .btn:hover {
    transform: translateY(-1px);
    background: rgba(232, 135, 131, 0.32);
    border-color: rgba(232, 135, 131, 0.55);
  }

  .btn-primary {
    border-color: var(--primary);
    background: var(--primary);
    color: var(--ink);
    padding: 12px 26px;
    font-size: 17px;
    box-shadow: 0 6px 16px rgba(196, 96, 92, 0.22);
  }

  .btn-primary:hover {
    background: color-mix(in srgb, var(--primary) 92%, var(--ink));
    border-color: color-mix(in srgb, var(--primary) 92%, var(--ink));
    box-shadow: 0 8px 20px rgba(196, 96, 92, 0.28);
  }

  .btn:focus-visible,
  .copy-inline:focus-visible {
    outline: 2px solid var(--primary);
    outline-offset: 2px;
  }

  .copy-inline {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    height: 40px;
    margin-left: auto;
    padding: 0 14px 0 38px;
    border: 1.5px solid rgba(232, 135, 131, 0.45);
    border-radius: 999px;
    background: rgba(232, 135, 131, 0.14);
    color: var(--primary-text);
    font-family: "Nunito", sans-serif;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.02em;
    cursor: pointer;
    flex: 0 0 auto;
    transition: transform 0.15s ease, background 0.15s ease, border-color 0.15s ease;
  }

  .copy-inline:hover {
    transform: translateY(-1px);
    background: rgba(232, 135, 131, 0.22);
    border-color: rgba(232, 135, 131, 0.6);
  }

  .copy-inline .copy-button-icon {
    width: 15px;
    height: 15px;
    left: 14px;
    top: 50%;
    transform: translateY(-50%);
  }

  .copy-inline.is-copied .copy-button-icon-copy {
    transform: translateY(-50%) scale(0.85);
  }

  .copy-inline .copy-button-icon-check {
    transform: translateY(-50%) scale(0.85);
  }

  .copy-inline.is-copied .copy-button-icon-check {
    transform: translateY(-50%) scale(1);
  }

  .copy-inline .copy-button-label {
    position: static;
    width: auto;
    height: auto;
    margin: 0;
    padding: 0;
    overflow: visible;
    clip: auto;
    border: 0;
    text-transform: lowercase;
  }

  .copy-inline.is-copied {
    background: rgba(155, 196, 182, 0.28);
    border-color: rgba(155, 196, 182, 0.6);
    color: rgba(22, 110, 80, 0.95);
  }

  /* Shared section header */
  .hero-block-head {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 14px;
  }

  .hero-block-label {
    font-family: var(--font-heading);
    font-size: 18px;
    font-weight: 600;
    letter-spacing: 0.01em;
    color: rgba(47, 42, 68, 0.9);
  }

  .hero-block-rule {
    flex: 1;
    height: 2px;
    border-radius: 999px;
    background: linear-gradient(90deg, rgba(232, 135, 131, 0.35), rgba(155, 196, 182, 0.25), transparent);
  }

  /* Workflow section */
  .workflow-section {
    margin-top: clamp(44px, 6vw, 58px);
  }

  .workflow-steps {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
    list-style: none;
    padding: 0;
  }

  .workflow-link {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 4px;
    padding: 24px 20px;
    text-decoration: none;
    border-radius: 20px;
    transition: background 0.15s ease;
  }

  .workflow-step:nth-child(1) .workflow-link { background: rgba(232, 135, 131, 0.06); }
  .workflow-step:nth-child(2) .workflow-link { background: rgba(155, 196, 182, 0.07); }
  .workflow-step:nth-child(3) .workflow-link { background: rgba(214, 166, 62, 0.06); }

  .workflow-orb {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 56px;
    height: 56px;
    border-radius: 50%;
    font-family: var(--font-heading);
    font-size: 24px;
    font-weight: 700;
    color: white;
    box-shadow: 0 2px 6px rgba(47, 42, 68, 0.15);
    margin-bottom: 12px;
  }

  .workflow-orb-1 { background: var(--primary); }
  .workflow-orb-2 { background: var(--secondary); }
  .workflow-orb-3 { background: var(--gold); }

  .workflow-link:hover {
    background: rgba(47, 42, 68, 0.06);
  }

  .workflow-link:hover .workflow-cmd {
    color: var(--accent);
  }


  .workflow-link:focus-visible {
    outline: 2px solid var(--primary);
    outline-offset: 2px;
    border-radius: 14px;
  }

  .workflow-cmd {
    font-family: "IBM Plex Mono", ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    font-size: 16px;
    font-weight: 700;
    color: var(--ink);
    background: none;
    padding: 0;
    transition: color 0.15s ease;
  }

  .workflow-desc {
    font-size: 14px;
    line-height: 1.35;
    color: var(--muted);
  }

  .cta-link:focus-visible {
    outline: 2px solid var(--primary);
    outline-offset: 2px;
  }

  /* Philosophy statement */
  .philosophy-section {
    margin-top: clamp(44px, 6vw, 58px);
  }

  .philosophy-label {
    font-family: "Caveat", cursive;
    font-weight: 500;
    font-size: clamp(30px, 5vw, 44px);
    line-height: 1;
    letter-spacing: 0.01em;
    color: var(--accent);
    margin-bottom: 14px;
    display: inline-block;
    transform: rotate(-1deg);
    transform-origin: left center;
  }

  .philosophy-text {
    font-family: var(--font-heading);
    font-size: clamp(20px, 3.5vw, 28px);
    font-weight: 600;
    line-height: 1.3;
    color: var(--muted);
  }

  .philosophy-text + .philosophy-text {
    margin-top: 24px;
  }

  .philosophy-text .accent-coral,
  .philosophy-text .accent-mint,
  .philosophy-text .accent-gold {
    color: var(--ink);
    padding: 1px 5px 2px;
    margin: 0 -1px;
    border-radius: 2px;
    -webkit-box-decoration-break: clone;
    box-decoration-break: clone;
  }

  .philosophy-text .accent-coral {
    background: rgba(232, 135, 131, 0.22);
  }

  .philosophy-text .accent-mint {
    background: rgba(155, 196, 182, 0.3);
  }

  .philosophy-text .accent-gold {
    background: rgba(214, 166, 62, 0.22);
  }

  .features-subcopy {
    max-width: 640px;
    margin: -4px 0 16px;
    color: var(--muted);
    font-size: 16px;
    line-height: 1.45;
  }

  /* Included pills (inside features section) */

  .included-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-bottom: 18px;
  }

  .included-tag {
    font-family: "IBM Plex Mono", ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    font-size: 13px;
    padding: 7px 14px;
    border-radius: 999px;
    border: 1.5px solid rgba(47, 42, 68, 0.12);
    background: none;
    color: rgba(47, 42, 68, 0.72);
    white-space: nowrap;
    cursor: pointer;
    transition: border-color 0.25s ease, background 0.25s ease, transform 0.2s ease, box-shadow 0.25s ease;
  }

  .included-tag:hover {
    transform: translateY(-1px);
    box-shadow: 0 2px 8px rgba(47, 42, 68, 0.1);
  }

  .included-tag:active {
    transform: translateY(0.5px);
    box-shadow: none;
  }

  .included-tag:focus-visible {
    outline: 2px solid var(--primary);
    outline-offset: 2px;
  }

  .included-tag.is-active {
    transform: translateY(-1px);
    box-shadow: 0 3px 10px rgba(47, 42, 68, 0.15);
  }

  .feature-link.is-muted {
    display: none;
  }

  .included-tag:nth-child(4n+1) {
    border-color: rgba(232, 135, 131, 0.35);
  }
  .included-tag:nth-child(4n+1):hover {
    border-color: rgba(232, 135, 131, 0.5);
  }
  .included-tag:nth-child(4n+1).is-active {
    border-color: rgba(232, 135, 131, 0.5);
    background: rgba(232, 135, 131, 0.16);
  }

  .included-tag:nth-child(4n+2) {
    border-color: rgba(155, 196, 182, 0.4);
  }
  .included-tag:nth-child(4n+2):hover {
    border-color: rgba(155, 196, 182, 0.55);
  }
  .included-tag:nth-child(4n+2).is-active {
    border-color: rgba(155, 196, 182, 0.55);
    background: rgba(155, 196, 182, 0.18);
  }

  .included-tag:nth-child(4n+3) {
    border-color: rgba(214, 166, 62, 0.35);
  }
  .included-tag:nth-child(4n+3):hover {
    border-color: rgba(214, 166, 62, 0.5);
  }
  .included-tag:nth-child(4n+3).is-active {
    border-color: rgba(214, 166, 62, 0.5);
    background: rgba(214, 166, 62, 0.16);
  }

  .included-tag:nth-child(4n+4) {
    border-color: rgba(47, 42, 68, 0.2);
  }
  .included-tag:nth-child(4n+4):hover {
    border-color: rgba(47, 42, 68, 0.35);
  }
  .included-tag:nth-child(4n+4).is-active {
    border-color: rgba(47, 42, 68, 0.35);
    background: rgba(47, 42, 68, 0.08);
  }

  /* Features section */
  .features-section {
    margin-top: clamp(44px, 6vw, 58px);
  }

  .performance-section {
    margin-top: clamp(44px, 6vw, 58px);
    padding: clamp(18px, 3vw, 30px) 0;
  }

  .performance-copy {
    width: 100%;
  }

  .performance-copy p {
    max-width: 1040px;
    margin: 0;
    color: var(--muted);
    font-size: clamp(18px, 2.6vw, 24px);
    font-weight: 700;
    line-height: 1.34;
  }

  .performance-link {
    display: inline;
    color: var(--ink);
    font-weight: 800;
    text-decoration: underline;
    text-decoration-color: rgba(232, 135, 131, 0.62);
    text-decoration-thickness: 2px;
    text-underline-offset: 4px;
    white-space: nowrap;
    transition: color 0.15s ease, text-decoration-color 0.15s ease;
  }

  .performance-link:hover {
    color: var(--accent);
    text-decoration-color: currentColor;
  }

  .performance-mark {
    display: inline;
    position: relative;
    isolation: isolate;
    color: var(--ink);
    font-weight: 900;
    padding: 0 0.16em 0.06em;
    margin: 0 -0.06em;
    border-radius: 2px;
    -webkit-box-decoration-break: clone;
    box-decoration-break: clone;
  }

  .performance-traffic {
    background: linear-gradient(to top, rgba(232, 135, 131, 0.28) 0 42%, transparent 42%);
  }

  .performance-clean {
    background: linear-gradient(to top, color-mix(in srgb, var(--secondary) 42%, transparent) 0 42%, transparent 42%);
  }

  .performance-copy a.performance-reference-link,
  .performance-copy a.performance-reference-link:visited {
    color: var(--ink);
    text-decoration: underline;
    text-decoration-color: rgba(232, 135, 131, 0.48);
    text-decoration-thickness: 1.5px;
    text-underline-offset: 3px;
    transition: color 0.15s ease, text-decoration-color 0.15s ease;
  }

  .performance-copy a.performance-reference-link:hover {
    color: var(--accent);
    text-decoration-color: currentColor;
  }

  .feature-list {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2px;
  }

  .feature-show-all {
    display: none;
  }

  @keyframes feature-reveal {
    from {
      opacity: 0;
      transform: translateY(12px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .feature-link {
    display: flex;
    flex-direction: column;
    justify-content: center;
    text-decoration: none;
    padding: 14px 16px;
    min-height: 76px;
    border-radius: 14px;
    transition: background 0.25s ease;
  }

  .feature-link:hover {
    background: rgba(232, 135, 131, 0.08);
  }

  .feature-link:hover .feature-link-title {
    color: var(--accent);
  }

  .feature-link-soon {
    opacity: 0.5;
    cursor: default;
  }

  .feature-link-soon:hover {
    background: none;
  }

  .feature-link-soon:hover .feature-link-title {
    color: var(--ink);
  }

  .soon-badge,
  .new-badge {
    display: inline-block;
    font-family: "Caveat", cursive;
    font-size: 18px;
    font-weight: 500;
    line-height: 1;
    vertical-align: baseline;
    margin-left: 4px;
  }

  .soon-badge {
    color: var(--primary-text);
  }

  .new-badge {
    color: var(--accent);
  }

  .feature-link:focus-visible {
    outline: 2px solid var(--primary);
    outline-offset: 2px;
  }

  .feature-link-title {
    display: block;
    font-family: var(--font-heading);
    font-size: 16px;
    font-weight: 600;
    color: var(--ink);
    line-height: 1.2;
    transition: color 0.15s ease;
  }

  .feature-link-desc {
    display: block;
    font-size: 14px;
    line-height: 1.35;
    color: var(--muted);
    margin-top: 2px;
  }

  .feature-link-desc code {
    font-family: "IBM Plex Mono", ui-monospace, monospace;
    font-size: 0.9em;
    padding: 1px 4px;
    border-radius: 4px;
    background: rgba(47, 42, 68, 0.06);
    color: var(--ink);
  }

  /* CTA section */
  .cta-section {
    margin-top: clamp(44px, 6vw, 58px);
    text-align: center;
    padding: 56px 0 28px;
    background:
      radial-gradient(ellipse at 45% 50%, rgba(232, 135, 131, 0.20) 0%, transparent 55%),
      radial-gradient(ellipse at 55% 50%, rgba(155, 196, 182, 0.17) 0%, transparent 55%);
  }

  .cta-text {
    font-family: var(--font-heading);
    font-size: clamp(24px, 4vw, 32px);
    font-weight: 700;
    color: var(--ink);
    margin-bottom: 20px;
  }

  .cta-text .mobile-break {
    display: none;
  }

  @media (max-width: 480px) {
    .cta-text .mobile-break {
      display: block;
    }
  }

  .cta-actions {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 16px;
    flex-wrap: wrap;
  }

  .cta-btn {
    padding: 14px 36px;
    font-size: 18px;
  }

  .cta-link {
    font-family: "Nunito", sans-serif;
    font-size: 15px;
    font-weight: 600;
    color: var(--ink);
    text-decoration: none;
    opacity: 0.7;
    transition: color 0.15s ease;
  }

  .cta-link:hover {
    color: var(--accent);
  }

  @keyframes blink {
    0%,
    50% { opacity: 1; }
    51%,
    100% { opacity: 0; }
  }

  @media (prefers-reduced-motion: reduce) {
    .cursor { animation: none; }
    .feature-list.is-expanded .feature-link:nth-child(n+7),
    .feature-list.is-expanded .feature-link-soon:nth-child(n+7) {
      animation: none;
    }
    .included-tag,
    .feature-link,
    .feature-link-title,
    .workflow-cmd,
    .btn,
    .performance-link,
    .performance-reference-link,
    .copy-inline,
    .cta-link { transition: none; }
  }

  @media (max-width: 900px) {
    .panel-hanger {
      right: 8px;
      top: 20px;
      transform: scale(0.9);
      transform-origin: top right;
    }
  }

  @media (max-width: 640px) {
    .title br {
      display: none;
    }

    .title .title-mid {
      display: block;
    }

    .title .accent {
      display: block;
    }

    .panel-hanger {
      right: -8px;
      top: 10px;
      transform: scale(0.78);
      transform-origin: top right;
    }

    .subtitle {
      color: var(--ink);
    }

    .hero::before {
      background:
        radial-gradient(ellipse at 80% 30%, rgba(155, 196, 182, 0.32) 0%, transparent 60%),
        radial-gradient(ellipse at 60% 70%, rgba(232, 135, 131, 0.28) 0%, transparent 55%),
        radial-gradient(ellipse at 90% 55%, rgba(214, 166, 62, 0.20) 0%, transparent 50%),
        radial-gradient(ellipse at 30% 40%, rgba(232, 135, 131, 0.13) 0%, transparent 60%),
        radial-gradient(ellipse at 50% 85%, rgba(155, 196, 182, 0.16) 0%, transparent 55%);
    }

    .command-card {
      display: none;
    }

    .hero-actions {
      margin-top: 4px;
      gap: 8px 10px;
    }

    .hero-actions .btn-primary {
      flex: 1 1 145px;
      min-width: 145px;
    }

    .hero-demo-link {
      flex: 1 1 145px;
      min-width: 145px;
    }

    .hero-actions-link:not(.hero-demo-link) {
      flex-basis: 100%;
      margin-top: 0;
      font-size: 14px;
      opacity: 0.62;
      text-align: center;
    }

    .workflow-link {
      flex-direction: row;
      align-items: center;
      text-align: left;
      gap: 12px;
      padding: 16px;
    }

    .workflow-cmd {
      min-width: 110px;
      flex-shrink: 0;
    }

    .workflow-orb {
      width: 36px;
      height: 36px;
      font-size: 16px;
      margin-bottom: 0;
      flex-shrink: 0;
    }

    .btn {
      padding: 10px 20px;
    }

    .btn-primary {
      padding: 13px 28px;
      font-size: 17px;
    }

    .feature-list {
      grid-template-columns: 1fr;
      gap: 6px;
    }

    .feature-link {
      border: 1.5px solid var(--line);
    }

    .feature-list:not(.is-expanded) .feature-link:nth-child(n+7),
    .feature-list:not(.is-expanded) .feature-link-soon:nth-child(n+7) {
      display: none;
    }

    .feature-list.is-expanded ~ .feature-show-all {
      display: none;
    }

    .feature-show-all {
      display: flex;
      align-items: center;
      gap: 8px;
      width: fit-content;
      margin: 14px auto 0;
      font: inherit;
      font-size: 14px;
      font-weight: 600;
      color: var(--muted);
      background: none;
      border: 1.5px solid var(--line);
      border-radius: 999px;
      padding: 9px 18px 9px 20px;
      cursor: pointer;
    }

    .feature-show-all-icon {
      width: 14px;
      height: 14px;
      stroke: currentColor;
      stroke-width: 2.5;
    }

    .feature-show-all:active {
      color: var(--primary-text);
      border-color: rgba(232, 135, 131, 0.45);
      background: rgba(232, 135, 131, 0.08);
    }

    .feature-show-all:focus-visible {
      outline: 2px solid var(--primary);
      outline-offset: 2px;
    }

    .feature-list.is-expanded .feature-link:nth-child(n+7),
    .feature-list.is-expanded .feature-link-soon:nth-child(n+7) {
      animation: feature-reveal 0.42s cubic-bezier(0.22, 1, 0.36, 1) both;
    }

    .feature-list.is-expanded > :nth-child(7)  { animation-delay: 0ms; }
    .feature-list.is-expanded > :nth-child(8)  { animation-delay: 45ms; }
    .feature-list.is-expanded > :nth-child(9)  { animation-delay: 90ms; }
    .feature-list.is-expanded > :nth-child(10) { animation-delay: 135ms; }
    .feature-list.is-expanded > :nth-child(11) { animation-delay: 180ms; }
    .feature-list.is-expanded > :nth-child(12) { animation-delay: 225ms; }
    .feature-list.is-expanded > :nth-child(13) { animation-delay: 270ms; }
    .feature-list.is-expanded > :nth-child(14) { animation-delay: 315ms; }
    .feature-list.is-expanded > :nth-child(15) { animation-delay: 360ms; }
    .feature-list.is-expanded > :nth-child(16) { animation-delay: 400ms; }
    .feature-list.is-expanded > :nth-child(17) { animation-delay: 440ms; }
    .feature-list.is-expanded > :nth-child(18) { animation-delay: 475ms; }
    .feature-list.is-expanded > :nth-child(19) { animation-delay: 505ms; }
    .feature-list.is-expanded > :nth-child(20) { animation-delay: 530ms; }
    .feature-list.is-expanded > :nth-child(21) { animation-delay: 555ms; }
    .feature-list.is-expanded > :nth-child(22) { animation-delay: 580ms; }
    .feature-list.is-expanded > :nth-child(23) { animation-delay: 605ms; }
    .feature-list.is-expanded > :nth-child(24) { animation-delay: 630ms; }

    .workflow-steps {
      grid-template-columns: 1fr;
    }

    .workflow-section,
    .philosophy-section,
    .features-section,
    .performance-section {
      margin-top: 34px;
    }

    .included-tags {
      gap: 8px;
    }

    .included-tag {
      font-size: 12px;
      padding: 6px 11px;
    }

    .hero-block-label {
      font-size: 16px;
    }

  }
</style>

<script is:inline type="module">
  const installCommand = "curl -fsSL https://tako.sh/install.sh | sh";
  const typedCmd = document.getElementById("typed-cmd");
  const copyBtn = document.getElementById("copy");
  const copyBtnLabel = copyBtn?.querySelector("[data-copy-label]");
  const hanger = document.getElementById("mit-hanger");
  const pin = document.getElementById("mit-pin");
  const tag = document.getElementById("mit-tag");

  let typingTimeout = null;

  const setCopyButtonText = (visible, aria) => {
    if (copyBtnLabel instanceof HTMLElement) {
      copyBtnLabel.textContent = visible;
    }
    if (copyBtn instanceof HTMLElement) {
      copyBtn.setAttribute("aria-label", aria);
      copyBtn.setAttribute("title", aria);
    }
  };

  const setCopyButtonCopied = (copied) => {
    if (copyBtn instanceof HTMLElement) {
      copyBtn.classList.toggle("is-copied", copied);
    }
  };

  const writeCommand = (command, animate) => {
    if (!(typedCmd instanceof HTMLElement)) {
      return;
    }

    if (typeof typingTimeout === "number") {
      clearTimeout(typingTimeout);
      typingTimeout = null;
    }

    if (!animate) {
      typedCmd.textContent = command;
      return;
    }

    typedCmd.textContent = "";
    let i = 0;
    const writeChar = () => {
      if (!(typedCmd instanceof HTMLElement) || i >= command.length) {
        return;
      }
      typedCmd.textContent += command.charAt(i);
      i += 1;
      typingTimeout = window.setTimeout(writeChar, 35 + Math.random() * 28);
    };
    typingTimeout = window.setTimeout(writeChar, 620);
  };

  writeCommand(installCommand, true);

  copyBtn?.addEventListener("click", async () => {
    try {
      await navigator.clipboard.writeText(installCommand);
      setCopyButtonCopied(true);
      setCopyButtonText("copied", "copied");
      window.posthog?.capture("install_command_copied");
      setTimeout(() => {
        setCopyButtonCopied(false);
        setCopyButtonText("copy", "copy install command");
      }, 1400);
    } catch {
      setCopyButtonCopied(false);
      setCopyButtonText("failed", "copy failed");
      setTimeout(() => {
        setCopyButtonText("copy", "copy install command");
      }, 1400);
    }
  });

  if (hanger instanceof HTMLElement && pin instanceof HTMLElement && tag instanceof HTMLElement) {
    const restAngle = 0;
    const isCompactScreen = window.matchMedia("(max-width: 640px)").matches;
    const restLength = isCompactScreen ? 54 : 62;
    const minLength = 30;
    const maxLength = 220;
    let angle = -0.42;
    let angularVelocity = 1.1;
    let length = restLength * 0.5;
    let lengthVelocity = 210;
    let dragging = false;
    let animating = false;
    const gravity = 1200;
    const angularDamping = 2.8;
    const angleSpring = 2.2;
    const lengthSpring = 19;
    const posePrecision = {
      angle: 0.001,
      length: 1,
      tilt: 0.001,
    };
    const settleThreshold = {
      angle: 0.002,
      angularVelocity: 0.02,
      length: 0.2,
      lengthVelocity: 2,
    };
    const lastPose = {
      angle: Number.NaN,
      length: Number.NaN,
      tilt: Number.NaN,
    };

    let lastFrame = performance.now();
    let dragSampleTime = lastFrame;
    let dragAngleSample = angle;
    let dragLengthSample = length;

    const clamp = (value, min, max) => Math.min(max, Math.max(min, value));
    const quantize = (value, step) => Math.round(value / step) * step;
    const formatRad = (value) => value.toFixed(3);
    const isNearRest = () => {
      return (
        Math.abs(angle - restAngle) < settleThreshold.angle &&
        Math.abs(angularVelocity) < settleThreshold.angularVelocity &&
        Math.abs(length - restLength) < settleThreshold.length &&
        Math.abs(lengthVelocity) < settleThreshold.lengthVelocity
      );
    };

    const applyPose = () => {
      const nextAngle = quantize(angle, posePrecision.angle);
      const nextLength = quantize(length, posePrecision.length);
      const nextTilt = quantize(-angle * 0.36, posePrecision.tilt);

      if (nextAngle !== lastPose.angle) {
        hanger.style.setProperty("--swing-angle", `${formatRad(nextAngle)}rad`);
        lastPose.angle = nextAngle;
      }
      if (nextLength !== lastPose.length) {
        hanger.style.setProperty("--thread-length", `${nextLength}px`);
        lastPose.length = nextLength;
      }
      if (nextTilt !== lastPose.tilt) {
        hanger.style.setProperty("--card-tilt", `${formatRad(nextTilt)}rad`);
        lastPose.tilt = nextTilt;
      }
    };

    const setFromPointer = (x, y) => {
      const rect = pin.getBoundingClientRect();
      const anchorX = rect.left + rect.width / 2;
      const anchorY = rect.top + rect.height / 2;
      const dx = x - anchorX;
      const dy = y - anchorY;
      angle = -Math.atan2(dx, dy);
      length = clamp(Math.hypot(dx, dy), minLength, maxLength);
    };

    const endDrag = (pointerId) => {
      if (!dragging) {
        return;
      }
      dragging = false;
      if (typeof pointerId === "number" && tag.hasPointerCapture(pointerId)) {
        tag.releasePointerCapture(pointerId);
      }
      angularVelocity = clamp(angularVelocity, -7.5, 7.5);
      lengthVelocity = clamp(lengthVelocity, -220, 220);
      tag.style.cursor = "grab";
      document.body.style.userSelect = "";
    };

    const startAnimation = () => {
      if (!animating && !window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
        animating = true;
        lastFrame = performance.now();
        requestAnimationFrame(tick);
      }
    };

    tag.addEventListener("pointerdown", (event) => {
      dragging = true;
      tag.setPointerCapture(event.pointerId);
      angularVelocity = 0;
      lengthVelocity = 0;
      setFromPointer(event.clientX, event.clientY);
      dragSampleTime = performance.now();
      dragAngleSample = angle;
      dragLengthSample = length;
      applyPose();
      startAnimation();
      tag.style.cursor = "grabbing";
      document.body.style.userSelect = "none";
    });

    tag.addEventListener("pointermove", (event) => {
      if (!dragging) {
        return;
      }
      setFromPointer(event.clientX, event.clientY);
      const now = performance.now();
      const dt = Math.max((now - dragSampleTime) / 1000, 1 / 240);
      angularVelocity = (angle - dragAngleSample) / dt;
      lengthVelocity = (length - dragLengthSample) / dt;
      dragSampleTime = now;
      dragAngleSample = angle;
      dragLengthSample = length;
      applyPose();
    });

    tag.addEventListener("pointerup", (event) => {
      endDrag(event.pointerId);
    });

    tag.addEventListener("pointercancel", (event) => {
      endDrag(event.pointerId);
    });

    tag.addEventListener("lostpointercapture", () => {
      dragging = false;
      tag.style.cursor = "grab";
      document.body.style.userSelect = "";
    });

    const tick = (now) => {
      const dt = Math.min((now - lastFrame) / 1000, 0.033);
      lastFrame = now;

      if (!dragging) {
        // Let the hanger fully settle by removing breeze in the near-rest band.
        const breeze = isNearRest() ? 0 : Math.sin(now * 0.0017) * 0.18 + Math.sin(now * 0.00061) * 0.08;
        const gravityForce = -(gravity / Math.max(length, 1)) * Math.sin(angle);
        const springForce = (restAngle - angle) * angleSpring;
        angularVelocity += (gravityForce + springForce + breeze - angularVelocity * angularDamping) * dt;
        angle += angularVelocity * dt;

        lengthVelocity += (restLength - length) * lengthSpring * dt;
        lengthVelocity *= Math.exp(-5.0 * dt);
        length += lengthVelocity * dt;
        length = clamp(length, minLength, maxLength);

        if (isNearRest()) {
          angle = restAngle;
          angularVelocity = 0;
          length = restLength;
          lengthVelocity = 0;
        }
      }

      applyPose();

      if (!dragging && isNearRest()) {
        animating = false;
      } else {
        requestAnimationFrame(tick);
      }
    };

    const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
    if (reducedMotion) {
      angle = restAngle;
      length = restLength;
      applyPose();
    } else {
      applyPose();
      animating = true;
      requestAnimationFrame(tick);
    }
  }

  // Feature filtering
  {
    const tags = Array.from(document.querySelectorAll(".included-tag[data-category]"));
    const features = Array.from(document.querySelectorAll(".feature-link[data-categories]"));
    const featureList = document.querySelector(".feature-list");
    const showAllBtn = document.querySelector("[data-feature-show-all]");
    let activeCategory = null;

    function setExpanded(expanded) {
      if (featureList) featureList.classList.toggle("is-expanded", expanded);
      if (showAllBtn) showAllBtn.style.display = expanded ? "none" : "";
    }

    function applyFilter(category) {
      activeCategory = category;

      if (!category) {
        for (const el of tags) el.classList.remove("is-active");
        for (const el of features) el.classList.remove("is-muted");
        setExpanded(false);
        return;
      }

      for (const el of tags) {
        el.classList.toggle("is-active", el.dataset.category === category);
      }

      for (const el of features) {
        const cats = (el.dataset.categories || "").split(",");
        el.classList.toggle("is-muted", !cats.includes(category));
      }

      setExpanded(true);
    }

    for (const t of tags) {
      t.addEventListener("click", () => {
        const category = t.dataset.category;
        if (activeCategory === category) {
          applyFilter(null);
        } else {
          applyFilter(category);
          window.posthog?.capture("feature_filter_applied", { category });
        }
      });
    }

    if (showAllBtn) {
      showAllBtn.addEventListener("click", () => setExpanded(true));
    }
  }

  // CTA button tracking
  for (const btn of document.querySelectorAll(".cta-section .btn, .hero-actions .btn")) {
    btn.addEventListener("click", () => {
      window.posthog?.capture("cta_clicked", {
        label: btn.textContent?.trim(),
        href: btn.getAttribute("href"),
      });
    });
  }

  // Workflow step tracking
  for (const link of document.querySelectorAll(".workflow-link")) {
    link.addEventListener("click", () => {
      const cmd = link.querySelector(".workflow-cmd")?.textContent?.trim();
      window.posthog?.capture("workflow_step_clicked", {
        command: cmd,
        href: link.getAttribute("href"),
      });
    });
  }

  // Feature link tracking
  for (const link of document.querySelectorAll(".feature-link[href]")) {
    link.addEventListener("click", () => {
      const title = link.querySelector(".feature-link-title")?.textContent?.trim();
      window.posthog?.capture("feature_link_clicked", {
        feature: title,
        href: link.getAttribute("href"),
      });
    });
  }
</script>
